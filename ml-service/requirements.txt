# ml-service/requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# fastapi      — the web framework. Similar to Express but for Python.
#                Handles routing, request parsing, response formatting.
#
# uvicorn      — the ASGI server that runs FastAPI (like how Node.js runs Express).
#                "ASGI" is the Python equivalent of Node's event loop model.
#
# pydantic     — data validation library. FastAPI uses it to validate request
#                bodies automatically. Wrong type? Missing field? → 422 error.
#
# python-multipart — needed for form data parsing (if you add file uploads later)
#
# OPTIONAL (add when upgrading to a real ML model):
# scikit-learn — the main ML library for training/loading models
# numpy        — numerical computing (required by scikit-learn)
# pandas       — data manipulation (for loading training datasets)
# joblib       — saving and loading trained models to/from .pkl files
# shap         — model explainability (produces feature importance scores)
# ─────────────────────────────────────────────────────────────────────────────

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Uncomment when adding a real ML model:
# scikit-learn==1.3.2
# numpy==1.26.2
# pandas==2.1.3
# joblib==1.3.2
# shap==0.43.0
